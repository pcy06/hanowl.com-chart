apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hanowl.fullname" . }}-test-connection"
  labels:
    {{- include "hanowl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hanowl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
